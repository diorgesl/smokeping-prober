# Secrets / environment
.env
.env.local
.env.*.local

# Runtime config managed by smokeping-manager (real targets + auto-backups)
config/config.yaml
config/backups/
# Router SSH credentials read by the prober (routers: password_file/private_key_file/known_hosts)
config/*.pass
config/*.key
config/known_hosts

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
